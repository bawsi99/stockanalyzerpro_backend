# Constraints file to explicitly exclude problematic packages
# Use with: pip install -r requirements.txt -c constraints.txt

# Explicitly exclude Jupyter packages in production
jupyterlab==0.0.0
jupyter==0.0.0
ipykernel==0.0.0
jupyter_client==0.0.0
jupyter_core==0.0.0
ipython==0.0.0
y-py==0.0.0
